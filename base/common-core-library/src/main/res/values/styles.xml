<resources>

    <!-- Base application theme. -->
    <style name="CoreLibrary.CommonAppTheme" parent="XUITheme.Phone">
        <!-- 自定义自己的主题样式 -->
        <item name="colorPrimary">@color/core_library_colorPrimary</item>
        <item name="colorPrimaryDark">@color/core_library_colorPrimaryDark</item>
        <item name="colorAccent">@color/core_library_colorAccent</item>

        <!--Slide-->
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>

    </style>

</resources>
